# using input function to get input from users
# the input is always a string
# we may need to convert format (even if they enter a number)
userName = input("Enter your name: ")
lenUserName = len(userName)
print(lenUserName)